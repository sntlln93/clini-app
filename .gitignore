.DS_Store
*.log
node_modules/
test-results/
playwright-report/
e2e/.auth/

# Local-only Claude Code session artifacts — only agents/, skills/ and
# settings.json are meant to be tracked.
.claude/docs/
.claude/transcripts/
.claude/handoffs/
.claude/session-report/

mocks/

# Duplicate skill copies the `skills` CLI writes for non-symlinking tools.
# `.agents/skills/` is the tracked copy; this one is redundant.
agent/
